fetchServer() modified

Changed fetchServer() and all the calls to it.
fetchServer() now takes a callback funtion which take as parameters the response data and status.

fetchServer(method, url, data, callback(res, status)) 